type Conta {
    conta: int!
    saldo: float!
}

type Query {
    contas: [Conta]!(model: "Conta")
}

type Mutation @group(middleware: [""]) {
    createConta(title: String!, body: String!): Conta @create(model: "Conta")
}
